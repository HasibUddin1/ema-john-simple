// USING FIREBASE ON A PROJECT

/**
 * 1. create a firebase project
 * 2. create web app
 * 3. npm install firebase
 * 4. save firebase config and export default app
 * 5. Build > Authentication > get started > enable sign in method
 * 6. create sign up and login
*/

/**
 * USING CONTEXT API FOR SHARING USER INFORMATION ON MULTIPLE COMPONENTS
 * 1. create a context provider file
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider
*/

// HOSTING
/**
 * --------------------
 * ONE TIME PER PC
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 
 * FOR EACH PROJECT ONE TIME
 * 1. firebase init
 * 2. proceed
 * 3. hosting: firebase (use space bar to select) 
 * 4. have to select an existing project if the project already exists (which will be the normal case for me)
 * 5. select the project carefully
 * 6. which project as public directory: dist
 * 7. single page application: yes
 * 8. continuous deployment: no
 * 
 * FOR EVERY TIME DEPLOY
 * 1. npm run build
 * 2. firebase deploy 
*/